# SBTI项目全替换指南

## 替换逻辑顺序

要完整替换SBTI项目中的人格、题目和维度，需要按照以下逻辑顺序进行修改，确保各部分之间的一致性：

### 1. 修改维度定义

维度是整个测试的基础，所有题目和人格都依赖于维度体系。

**修改位置**：`dimensionMeta`对象（index.html第785-801行）

**步骤**：
1. 确定新的维度体系（建议保持15个维度，与原结构一致，便于后续匹配规则的设置）
2. 为每个维度定义名称和所属模型
3. 更新`dimensionOrder`数组（index.html第1472行），确保维度顺序与后续匹配规则对应

**示例**：
```javascript
const dimensionMeta = {
  N1: { name: 'N1 新维度1', model: '新模型1' },
  N2: { name: 'N2 新维度2', model: '新模型1' },
  // ... 共15个维度
};

const dimensionOrder = ['N1','N2',...]; // 15个维度的顺序
```

### 2. 替换题目

题目需要与新的维度体系对应，每个维度建议保持2个题目。

**修改位置**：`questions`数组（index.html第802-1072行）

**步骤**：
1. 为每个新维度创建2个题目
2. 确保每个题目包含正确的`id`、`dim`（对应新维度）、`text`和`options`
3. 选项值保持1-3分的范围
4. 如有需要，修改`specialQuestions`数组（index.html第1074-1096行）中的特殊问题

**示例**：
```javascript
const questions = [
  {
    id: 'q1', dim: 'N1',
    text: '新问题1',
    options: [
      { label: '选项1', value: 1 },
      { label: '选项2', value: 2 },
      { label: '选项3', value: 3 }
    ]
  },
  // ... 共30个题目（15个维度×2）
];
```

### 3. 定义新的人格类型

人格类型是测试的核心输出，需要与新的维度体系匹配。

**修改位置**：`TYPE_LIBRARY`对象（index.html第1099-1261行）

**步骤**：
1. 为每个新人格定义`code`、`cn`（中文名称）、`intro`（介绍语）和`desc`（详细描述）
2. 确保人格代码唯一且易于识别
3. 为每个人格准备对应的图片文件

**示例**：
```javascript
const TYPE_LIBRARY = {
  NEW1: {
    code: "NEW1",
    cn: "新人格1",
    intro: "新人格介绍语",
    desc: "新人格详细描述..."
  },
  // ... 多个新人格
};
```

### 4. 更新人格图片映射

为新人格添加对应的图片路径。

**修改位置**：`TYPE_IMAGES`对象（index.html第1263-1291行）

**步骤**：
1. 为每个新人格添加图片路径
2. 确保图片文件已添加到`image/`文件夹

**示例**：
```javascript
const TYPE_IMAGES = {
  "NEW1": "./image/NEW1.png",
  // ... 其他新人格图片
};
```

### 5. 设置新的人格匹配规则

匹配规则需要根据新的维度体系和人格特点进行设置。

**修改位置**：`NORMAL_TYPES`数组（index.html第1293-1393行）

**步骤**：
1. 为每个新人格创建维度模式
2. 模式长度为15个字符，对应`dimensionOrder`中的维度顺序
3. 使用L（低）、M（中）、H（高）表示各维度的等级

**示例**：
```javascript
const NORMAL_TYPES = [
  {
    "code": "NEW1",
    "pattern": "HHH-MHM-LHH-HHH-MHM" // 15个字符，对应15个维度
  },
  // ... 其他新人格规则
];
```

### 6. 更新维度解释

维度解释需要与新的维度体系对应，为每个维度的三个等级提供解释。

**修改位置**：`DIM_EXPLANATIONS`对象（index.html第1395-1470行）

**步骤**：
1. 为每个新维度添加L、M、H三个等级的解释
2. 确保解释与维度含义一致

**示例**：
```javascript
const DIM_EXPLANATIONS = {
  "N1": {
    "L": "低等级解释",
    "M": "中等级解释",
    "H": "高等级解释"
  },
  // ... 其他新维度解释
};
```

### 7. 处理特殊情况

如果需要保留特殊人格（如酒鬼人格），需要相应修改特殊问题和触发逻辑。

**修改位置**：
- `specialQuestions`数组（index.html第1074-1096行）
- `DRUNK_TRIGGER_QUESTION_ID`常量（index.html第1474行）
- `getDrunkTriggered`函数（index.html第1601-1603行）
- `computeResult`函数中的特殊情况处理（index.html第1646-1658行）

### 8. 测试验证

修改完成后，需要进行测试验证，确保：
1. 所有题目能够正常显示和提交
2. 人格匹配逻辑正确
3. 结果页面显示正常
4. 图片加载无误

## 注意事项

1. **一致性**：确保维度、题目、人格、匹配规则和解释之间的一致性
2. **格式**：保持代码格式的正确性，特别是JSON对象和数组的结构
3. **图片**：为每个人格准备合适的图片，并确保路径正确
4. **测试**：在修改过程中定期测试，及时发现并修复问题
5. **备份**：在修改前备份原文件，以防出现问题时可以恢复

## 总结

按照上述逻辑顺序进行修改，可以确保SBTI项目的所有人格、题目和维度被完整替换，同时保持系统的功能完整性。修改过程中需要注意各部分之间的关联关系，确保修改后系统能够正常运行。