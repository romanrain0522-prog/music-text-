# SBTI项目分析与自定义指南

## 项目概述

SBTI（Spaceship BTI）是一个基于网页的人格测试工具，类似于MBTI但具有更具娱乐性的人格类型和问题设计。项目由B站up主蛆肉儿串儿（UID417038183）创建，托管在Cloudflare上。

### 项目结构

- **主文件**：index.html（包含所有HTML、CSS和JavaScript代码）
- **图片文件夹**：image/（存放各个人格类型的图片）

## 如何自定义题目和人格

### 自定义题目

题目定义在`questions`数组中（index.html第802-1072行），每个题目对象包含以下属性：

```javascript
{
  id: 'q1',          // 题目ID
  dim: 'S1',         // 所属维度
  text: '问题文本',   // 题目内容
  options: [         // 选项数组
    { label: '选项1', value: 1 },
    { label: '选项2', value: 2 },
    { label: '选项3', value: 3 }
  ]
}
```

**修改方法**：
1. 直接编辑`questions`数组中的题目文本
2. 修改选项内容和对应分值
3. 如需添加新题目，按照相同格式添加到数组中
4. 注意保持题目与维度的对应关系

### 自定义人格类型

人格类型定义在`TYPE_LIBRARY`对象中（index.html第1099-1261行），每个人格对象包含以下属性：

```javascript
"CTRL": {
  "code": "CTRL",           // 人格代码
  "cn": "拿捏者",          // 中文名称
  "intro": "怎么样，被我拿捏了吧？",  // 介绍语
  "desc": "恭喜您，您测出了全中国最为罕见的人格..."  // 详细描述
}
```

**修改方法**：
1. 编辑现有人格的名称、介绍和描述
2. 添加新人格到`TYPE_LIBRARY`对象
3. 为新人格添加对应图片到image文件夹
4. 在`TYPE_IMAGES`对象中添加新人格的图片路径

### 自定义人格匹配规则

人格匹配规则定义在`NORMAL_TYPES`数组中（index.html第1293-1393行），每个规则包含：

```javascript
{
  "code": "CTRL",                 // 人格代码
  "pattern": "HHH-HMH-MHH-HHH-MHM"  // 维度模式
}
```

**维度模式说明**：
- 由15个字符组成，对应15个维度
- 每个字符表示该维度的等级：L（低）、M（中）、H（高）
- 维度顺序：S1,S2,S3,E1,E2,E3,A1,A2,A3,Ac1,Ac2,Ac3,So1,So2,So3

**修改方法**：
1. 调整现有人格的维度模式
2. 为新人格添加维度模式

### 自定义维度解释

维度解释定义在`DIM_EXPLANATIONS`对象中（index.html第1395-1470行），每个维度包含三个等级的解释：

```javascript
"S1": {
  "L": "对自己下手比别人还狠，夸你两句你都想先验明真伪。",
  "M": "自信值随天气波动，顺风能飞，逆风先缩。",
  "H": "心里对自己大致有数，不太会被路人一句话打散。"
}
```

**修改方法**：
- 编辑现有维度的解释文本
- 如需添加新维度，需要同时修改多个相关部分

## 评分规则

### 维度评分

1. **原始得分**：每个维度有2个问题，每个问题得分1-3分，维度总得分范围为2-6分
2. **等级转换**：
   - 得分 ≤ 3 → L（低）
   - 得分 = 4 → M（中）
   - 得分 ≥ 5 → H（高）

### 人格匹配

1. **计算用户向量**：将15个维度的等级转换为数字（L=1, M=2, H=3）
2. **计算距离**：与每个人格的维度模式计算欧氏距离
3. **排序**：按距离从小到大排序，距离越小匹配度越高
4. **匹配度计算**：`similarity = Math.max(0, Math.round((1 - distance / 30) * 100))`

### 特殊情况

1. **酒鬼人格**：如果回答特殊问题时选择了饮酒相关选项，直接返回DRUNK人格
2. **傻乐者人格**：如果最高匹配度低于60%，返回HHHH（傻乐者）人格

## 测试流程

1. **首页**：点击"开始测试"按钮
2. **答题**：回答30个常规问题和1个特殊问题（可能触发第2个特殊问题）
3. **提交**：完成所有问题后点击"提交并查看结果"
4. **结果**：显示匹配的人格类型、匹配度、维度评分和详细解释

## 技术实现

- **前端框架**：纯HTML、CSS和JavaScript
- **样式**：使用CSS变量和Flexbox/Grid布局
- **交互**：原生JavaScript事件处理
- **数据存储**：使用浏览器内存存储答案
- **图片**：使用本地图片文件

## 注意事项

1. 本测试仅供娱乐，不具有专业心理学依据
2. 修改题目和人格时，注意保持代码格式的正确性
3. 添加新人格时，确保同时添加对应的图片文件
4. 维度模式的修改需要了解各维度的含义和相互关系
5. 测试结果可能会受到问题顺序和用户回答随机性的影响

## 项目文件

- [index.html](file:///workspace/SBTI/index.html)：主文件
- [image/](file:///workspace/SBTI/image)：人格图片文件夹

## 总结

SBTI是一个设计精巧的娱乐性人格测试工具，通过简单的HTML、CSS和JavaScript实现了完整的测试流程。自定义题目和人格非常直观，只需要修改相应的数组和对象即可。项目的评分规则基于维度得分和模式匹配，逻辑清晰易懂。

通过本指南，您可以轻松地修改测试题目、添加新的人格类型，或者调整评分规则，以适应不同的测试需求和场景。